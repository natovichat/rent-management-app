{
  "buildCommand": "cd ../.. && npm run build --workspace=apps/frontend",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/properties/:id",
      "destination": "/properties/[id]"
    }
  ]
}
