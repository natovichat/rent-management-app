rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client access - backend uses Admin SDK which bypasses these rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
